# Configuration de l'Assistant IA
# Tu peux modifier ce fichier sans toucher au code !

# === LLM (Cerveau) ===
llm:
  # Quel type de LLM utiliser : "ollama", "openai", "groq"
  provider: "ollama"
  
  # Configuration Ollama
  ollama:
    model: "llama3.2:3b"      # Modèle à utiliser
    base_url: "http://localhost:11434"
  
  # Configuration OpenAI (pour plus tard)
  # openai:
  #   model: "gpt-4"
  #   api_key: "sk-..."  # Ou utiliser variable d'environnement

# === Personnalité de l'assistant ===
character:
  name: "Aria"
  # Le "system prompt" définit la personnalité de l'IA
  system_prompt: |
    Tu es Aria, une assistante IA amicale et serviable.
    Tu réponds de manière concise et claire en français.
    Tu es enthousiaste et positive.
    Tu aides l'utilisateur dans ses tâches quotidiennes.

# === TTS (Voix) ===
tts:
  # Provider TTS par défaut : "kokoro" (local, naturel) ou "edge" (cloud, gratuit)
  provider: "kokoro"
  
  # --- Kokoro TTS (100% local, haute qualité) ---
  # Voix disponibles: ff_siwis (FR), af_heart (US), bf_emma (UK), jf_alpha (JP)
  kokoro_voice: "ff_siwis"
  
  # --- Edge TTS (cloud Microsoft, fallback) ---
  voice: "fr-FR-DeniseNeural"
  # Autres voix Edge:
  # - fr-FR-HenriNeural (homme français)
  # - en-US-JennyNeural (femme américaine)  
  # - ja-JP-NanamiNeural (femme japonaise)
  
  rate: "+20%"   # Vitesse Edge TTS (-50% à +100%)
  pitch: "+0Hz"  # Hauteur Edge TTS (-50Hz à +50Hz)

# === ASR (Reconnaissance vocale) ===
asr:
  # Provider ASR disponibles :
  # - "whisper"  : Local, compatible CPU/GPU, bon pour le français avec modèles distillés
  # - "canary"   : NVIDIA Canary 1B v2, meilleur pour longs audios (6GB+ VRAM requis)
  # - "parakeet" : NVIDIA Parakeet TDT 0.6B v3, rapide et précis (2GB VRAM) ⭐ RECOMMANDÉ
  provider: "parakeet"
  
  # ─────────────────────────────────────────────────────────────────────────
  # 🦜 PARAKEET TDT 0.6B V3 (NVIDIA) - Rapide et précis ⭐ RECOMMANDÉ
  # ─────────────────────────────────────────────────────────────────────────
  # - 600M params, ~2GB VRAM (léger!)
  # - Token-and-Duration Transducer (TDT) architecture
  # - Excellent sur audios courts ET longs
  # - 25 langues supportées (dont français, anglais, espagnol...)
  # - Détection automatique de la langue
  # - Plus rapide que Canary et Whisper large
  # - REQUIERT un GPU NVIDIA
  # ─────────────────────────────────────────────────────────────────────────
  
  # ─────────────────────────────────────────────────────────────────────────
  # 🐤 CANARY 1B V2 (NVIDIA) - State-of-the-art ASR
  # ─────────────────────────────────────────────────────────────────────────
  # - Meilleure précision que Whisper (WER ~5% pour le français)
  # - 25 langues européennes supportées
  # - Ponctuation et majuscules automatiques
  # - REQUIERT un GPU NVIDIA avec 6GB+ VRAM
  # - Plus rapide que Whisper sur GPU
  # ⚠️ Moins bon sur les phrases courtes conversationnelles
  # ─────────────────────────────────────────────────────────────────────────
  
  # Modèles Whisper disponibles (si provider: "whisper"):
  # ─────────────────────────────────────────────────────────────────────────
  # MODÈLES STANDARD (téléchargement auto depuis OpenAI):
  # - tiny   : 39M params, très rapide, faible qualité
  # - base   : 74M params, rapide, qualité moyenne
  # - small  : 244M params, BON COMPROMIS! Léger + bonne qualité
  # - medium : 769M params, lent, très bonne qualité
  # - large-v3: 1.5B params, très lent, meilleure qualité
  # - turbo  : 809M params, 6x plus rapide que large-v3 (mais hallucine parfois)
  # - distil-large-v3: 756M params, rapide mais ANGLAIS SEULEMENT
  # ─────────────────────────────────────────────────────────────────────────
  # 🇫🇷 MODÈLES FRANÇAIS OPTIMISÉS (téléchargés dans models/, gitignored):
  # - french-distil-dec4 : 0.8B params, Excellente qualité FR, ~500MB download
  # - french-distil-dec2 : 0.8B params, Plus rapide, très bonne qualité FR
  # Ces modèles sont fine-tunés sur 2500+ heures de français par bofenghuang.
  # WER: 8.37% (bien meilleur que Whisper standard pour le français)
  # ─────────────────────────────────────────────────────────────────────────
  model_size: "french-distil-dec2"  # Modèle français optimisé - rapide et précis
  
  # Langue: "fr", "en", "auto" ou vide "" pour auto-détection
  # Pour les modèles français, laisser "fr" pour de meilleurs résultats.
  language: "fr"
  
  # Prompt pour guider la transcription (aide même avec langue forcée)
  # Donne du contexte sur le style de conversation attendu.
  # Note: Utilisé uniquement par Whisper, Canary n'utilise pas de prompt.
  prompt: "Transcription d'une conversation en français avec une assistante IA."
  
  # Device: "cpu" ou "cuda" (GPU)
  # Pour Canary: "cuda" FORTEMENT recommandé (6x plus rapide)
  # Pour Whisper: "cpu" fonctionne bien, "cuda" pour plus de vitesse
  device: "cuda"
