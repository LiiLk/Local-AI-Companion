# AI Assistant Configuration
# You can modify this file without touching the code!

# === LLM (Brain) ===
llm:
  # Which LLM provider to use: "ollama" or "llamacpp"
  # - "ollama"   : Simple, pre-packaged models (llama3.2, mistral, etc.)
  # - "llamacpp" : Flexible, supports vision models (Qwen3-VL, LLaVA, etc.)
  provider: "llamacpp"
  
  # ─────────────────────────────────────────────────────────────────────────
  # 🦙 LLAMA.CPP SERVER (for Qwen3-VL and vision models)
  # ─────────────────────────────────────────────────────────────────────────
  # Qwen3-VL-8B is a state-of-the-art vision-language model.
  # It uses Vulkan for GPU (no CUDA Toolkit needed).
  # 
  # Start the server before launching the assistant:
  #   ./scripts/start_llm_server.sh
  # or manually:
  #   cd ~/tools/llama-cpp && LD_LIBRARY_PATH="$PWD" ./llama-server \
  #     --model models/qwen3-vl-8b/Qwen3-VL-8B-Instruct-Q4_K_M.gguf \
  #     --mmproj models/qwen3-vl-8b/mmproj-F16.gguf \
  #     --host 0.0.0.0 --port 8080 --ctx-size 8192 --n-gpu-layers 99 \
  #     --jinja --no-context-shift
  # ─────────────────────────────────────────────────────────────────────────
  llamacpp:
    base_url: "http://localhost:8080"
    model_name: "qwen3-vl-8b-instruct"
    # Recommended parameters for Qwen3-VL
    max_tokens: 4096        # Qwen3-VL supports long generation
    temperature: 0.7        # Standard temperature for Qwen models
    top_p: 0.9              # Top-p sampling
    top_k: 40               # Top-k sampling  
    presence_penalty: 1.1   # Reduces repetitions
  
  # Ollama configuration (fallback)
  ollama:
    model: "llama3.2:3b"      # Model to use
    base_url: "http://localhost:11434"

# === Assistant Personality ===
# ─────────────────────────────────────────────────────────────────────────
# 🎭 CHARACTER PRESETS
# ─────────────────────────────────────────────────────────────────────────
# The project is fully modular! You can switch characters by:
# 1. Using a preset: set character.preset to load from config/characters/
# 2. Custom config: set character.name and character.system_prompt directly
#
# Available presets (in config/characters/):
# - "march7th" : March 7th from Honkai: Star Rail (cheerful photographer)
# - "juri"     : Juri Han from Street Fighter (sarcastic villain)
# - "clippy"   : Microsoft Clippy (helpful paperclip)
#
# To add a new character:
# 1. Create config/characters/yourcharacter.yaml
# 2. Add Live2D model to assets/models/yourcharacter/
# 3. Add voice reference to resources/voices/yourcharacter/
# 4. Set character.preset: "yourcharacter" below
# ─────────────────────────────────────────────────────────────────────────
character:
  # Use a preset (loads from config/characters/<preset>.yaml)
  # Set to null or remove to use custom config below
  preset: "march7th"
  
  # Custom config (used if preset is null)
  # name: "My Character"
  # system_prompt: |
  #   Your custom personality prompt here...

# === TTS (Voice) ===
tts:
  # Default TTS provider:
  # - "cosyvoice3": CosyVoice3 0.5B (BEST quality, 9 languages, zero-shot cloning) ⭐ BEST QUALITY
  # - "xtts"      : XTTS v2 (voice cloning, 17 languages, ~2.8GB VRAM)
  # - "kokoro"    : Kokoro 82M (local, natural, lightweight, no voice cloning)
  # - "f5tts"     : F5-TTS (Emotional support, ~4GB VRAM)
  # - "edge"      : Edge TTS (Microsoft cloud, free)
  provider: "cosyvoice3"
  
  # ─────────────────────────────────────────────────────────────────────────
  # 🎤 COSYVOICE3 0.5B (State-of-the-Art) ⭐ RECOMMENDED FOR QUALITY
  # ─────────────────────────────────────────────────────────────────────────
  # - Zero-shot voice cloning with any reference audio
  # - LLM 0.5B + Flow 1.3GB + HiFi-GAN → ~2.5GB VRAM in FP16 ⚠️
  #   (Note: "0.5B params" is only the LLM, total model is ~4GB on disk)
  # - Fits with Qwen3-VL (~7GB) = ~9.5GB total ✅
  # - 9 native languages: EN, ZH, JA, KO, DE, ES, FR, IT, RU
  # - ~150ms latency (faster than XTTS!)
  # - Apache 2.0 license (commercial use OK)
  # - Outperforms F5-TTS, XTTS v2 in benchmarks (WER 0.81, Similarity 77.4%)
  # - Uses RL-tuned model (llm.rl.pt) for better quality ⭐
  #
  # BEST PRACTICES (from official documentation):
  # - Reference audio: 3-10 seconds of clean speech
  # - prompt_text: Exact transcription of reference for zero-shot mode
  # - Modes: "zero_shot" (best cloning), "cross_lingual" (different lang), "instruct" (control)
  #
  # SETUP:
  # 1. Start server: ./scripts/start_cosyvoice3.sh start
  # 2. Reference audio prepared at resources/voices/
  # 3. Server runs in separate conda env (Python 3.10 required)
  # ─────────────────────────────────────────────────────────────────────────
  cosyvoice3:
    # API URL of the CosyVoice3 server
    # Start server with: ./scripts/start_cosyvoice3.sh start
    api_url: "http://127.0.0.1:9881"

    # Reference audio for voice cloning (path to .wav file)
    # CosyVoice3 works best with 3-10 seconds of clean speech ⭐
    # Using march7th_short.wav (5.5s, 24kHz) for optimal quality
    ref_audio_path: "resources/voices/march7th/march7th_short.wav"

    # Transcription of the reference audio (for better zero-shot cloning)
    # This text MUST match exactly what is said in the reference audio!
    # The transcription enables "zero_shot" mode which gives best voice similarity
    prompt_text: "Every time I'm done sorting my album and look out the window, I always want to take another picture."

    # Inference mode: "auto", "zero_shot", "cross_lingual", or "instruct"
    # - auto: Uses zero_shot if prompt_text provided, else cross_lingual
    # - zero_shot: Best voice cloning when prompt_text matches reference exactly
    # - cross_lingual: Best for different language than reference (ref=EN, output=FR)
    # - instruct: Explicit language/dialect/emotion control
    #
    # TIP: Use "cross_lingual" when reference audio language differs from output
    mode: "cross_lingual"

    # Default language: fr, en, ja, ko, de, es, it, ru, zh
    language: "fr"

    # Speech speed factor (default: 1.0)
    speed: 1.0
  
  # ─────────────────────────────────────────────────────────────────────────
  # ⚡ STREAMING TTS (sentence-by-sentence)
  # ─────────────────────────────────────────────────────────────────────────
  # When enabled, TTS starts synthesizing as soon as a sentence is complete,
  # without waiting for the full LLM response. This dramatically reduces
  # perceived latency.
  # - true: Synthesize sentence by sentence (faster perceived response)
  # - false: Wait for full response before TTS (simpler, more stable)
  # ─────────────────────────────────────────────────────────────────────────
  stream_tts: true
  
  # ─────────────────────────────────────────────────────────────────────────
  # 🌐 AUTO LANGUAGE DETECTION
  # ─────────────────────────────────────────────────────────────────────────
  # When enabled, the TTS will automatically detect the language of the text
  # and use the appropriate voice configuration.
  # Works with: XTTS (changes language parameter), Kokoro (changes voice),
  #             Edge TTS (changes voice)
  # ─────────────────────────────────────────────────────────────────────────
  auto_detect_language: true
  
  # ─────────────────────────────────────────────────────────────────────────
  # 🗣️ VOICE MAPPING BY LANGUAGE
  # ─────────────────────────────────────────────────────────────────────────
  # Define which voice to use for each language.
  # Used when auto_detect_language is enabled.
  # ─────────────────────────────────────────────────────────────────────────
  voice_mapping:
    # F5-TTS voices by language (not currently used, kept for future)
    # f5tts:
    #   fr: "f5_march7th_fr"
    #   en: "f5_march7th_en"

    # XTTS v2 voices by language (speaker name)
    xtts:
      fr: "Claribel Dervla"     # Works great for French
      en: "Claribel Dervla"     # Same voice, multilingual
      # Or use different speakers:
      # fr: "Zacharie Aimilios"  # French male voice
      # en: "Ana Florence"       # English female voice
    
    # Kokoro voices by language
    kokoro:
      fr: "ff_siwis"    # French female voice
      en: "af_heart"    # American female voice
    
    # Edge TTS voices by language
    edge:
      fr: "fr-FR-DeniseNeural"   # French female voice
      en: "en-US-JennyNeural"    # American female voice
  
  # ─────────────────────────────────────────────────────────────────────────
  # 🎤 XTTS v2 (Voice Cloning) - Best multilingual choice ⭐ RECOMMENDED
  # ─────────────────────────────────────────────────────────────────────────
  # - ~467M params, ~2.8GB VRAM (coexists with Qwen3-VL ~7GB = ~10GB total ✅)
  # - Voice cloning with only 6 seconds of audio!
  # - 17 native languages: FR, EN, ES, DE, IT, PT, PL, TR, RU, NL, CS, AR, ZH, JA, HU, KO, HI
  # - 58 high-quality built-in speakers
  # - Streaming with latency < 200ms
  # - Model downloaded automatically (~1.9GB)
  # ─────────────────────────────────────────────────────────────────────────
  xtts:
    # Default language (used when auto_detect_language is false)
    language: "en"
    
    # Device: "cuda" for GPU acceleration (~2.8GB VRAM)
    # With Qwen3-VL (~7GB) + XTTS (~2.8GB) = ~10GB total → fits in 12GB!
    # Set to "cpu" if you get OOM errors
    device: "cuda"
    
    # Built-in speaker (used if no voice cloning and no voice_mapping)
    # Some recommended voices:
    # - "Claribel Dervla" : Clear and natural female voice
    # - "Ana Florence" : Soft female voice
    # - "Craig Gutsy" : Energetic male voice
    # - "Zacharie Aimilios" : French male voice
    # - "Filip Traverse" : French male voice
    speaker: "Claribel Dervla"
    
    # 🎭 Voice Cloning: DISABLED (needs real human voice recording)
    # ─────────────────────────────────────────────────────────────────────────
    # Voice cloning requires a REAL human voice recording (not TTS-generated!)
    # The march7th_optimized.wav was generated by TTS, which causes poor results.
    # To enable voice cloning:
    # 1. Get real voice recordings (e.g., from game files or real recordings)
    # 2. Process: 24kHz, mono, clean speech, 10-30 seconds
    # 3. Uncomment the speaker_wav line below
    # ─────────────────────────────────────────────────────────────────────────
    # speaker_wav: "resources/voices/march7th/march7th_real.wav"
  
  # ─────────────────────────────────────────────────────────────────────────
  # 🎵 KOKORO TTS (100% local, lightweight, no voice cloning)
  # ─────────────────────────────────────────────────────────────────────────
  # - 82M params, ~300MB, natural quality
  # - Voices: ff_siwis (FR), af_heart (US), bf_emma (UK), jf_alpha (JP)
  # ─────────────────────────────────────────────────────────────────────────
  kokoro_voice: "ff_siwis"
  
  # Voice mapping for multi-language switching
  voice_mapping:
    kokoro:
      fr: "ff_siwis"
      en: "af_heart"
      jp: "jf_alpha"
  
  # ─────────────────────────────────────────────────────────────────────────
  # ☁️ EDGE TTS (Microsoft cloud, fallback)
  # ─────────────────────────────────────────────────────────────────────────
  voice: "en-US-JennyNeural"
  # Other Edge voices:
  # - en-US-GuyNeural (American male)
  # - en-GB-SoniaNeural (British female)
  # - fr-FR-DeniseNeural (French female)
  # - ja-JP-NanamiNeural (Japanese female)
  
  rate: "+0%"    # Edge TTS speed (-50% to +100%)
  pitch: "+0Hz"  # Edge TTS pitch (-50Hz to +50Hz)

# === ASR (Speech Recognition) ===
asr:
  # Available ASR providers:
  # - "whisper"  : Local, CPU/GPU compatible, good for many languages with distilled models
  # - "canary"   : NVIDIA Canary 1B v2, best for long audio (6GB+ VRAM required)
  # - "parakeet" : NVIDIA Parakeet TDT 0.6B v3, fast and accurate (can run on CPU)
  # ⚠️ With Qwen3-VL (~7GB VRAM), use ASR on CPU to avoid OOM
  provider: "whisper"
  
  # ─────────────────────────────────────────────────────────────────────────
  # 🦜 PARAKEET TDT 0.6B V3 (NVIDIA) - Fast and accurate ⭐ RECOMMENDED
  # ─────────────────────────────────────────────────────────────────────────
  # - 600M params, ~2GB VRAM (lightweight!)
  # - Token-and-Duration Transducer (TDT) architecture
  # - Excellent on both short AND long audio
  # - 25 languages supported (including French, English, Spanish...)
  # - Automatic language detection
  # - Faster than Canary and Whisper large
  # - REQUIRES an NVIDIA GPU
  # ─────────────────────────────────────────────────────────────────────────
  
  # ─────────────────────────────────────────────────────────────────────────
  # 🐤 CANARY 1B V2 (NVIDIA) - State-of-the-art ASR
  # ─────────────────────────────────────────────────────────────────────────
  # - Better accuracy than Whisper (WER ~5% for French)
  # - 25 European languages supported
  # - Automatic punctuation and capitalization
  # - REQUIRES an NVIDIA GPU with 6GB+ VRAM
  # - Faster than Whisper on GPU
  # ⚠️ Less good on short conversational phrases
  # ─────────────────────────────────────────────────────────────────────────
  
  # Available Whisper models (if provider: "whisper"):
  # ─────────────────────────────────────────────────────────────────────────
  # STANDARD MODELS (auto-download from OpenAI):
  # - tiny   : 39M params, very fast, low quality
  # - base   : 74M params, fast, medium quality
  # - small  : 244M params, GOOD COMPROMISE! Lightweight + good quality
  # - medium : 769M params, slow, very good quality
  # - large-v3: 1.5B params, very slow, best quality
  # - turbo  : 809M params, 6x faster than large-v3 (but sometimes hallucinates)
  # - distil-large-v3: 756M params, fast but ENGLISH ONLY
  # ─────────────────────────────────────────────────────────────────────────
  # 🇫🇷 FRENCH-OPTIMIZED MODELS (downloaded to models/, gitignored):
  # - french-distil-dec4 : 0.8B params, Excellent FR quality, ~500MB download
  # - french-distil-dec2 : 0.8B params, Faster, very good FR quality
  # These models are fine-tuned on 2500+ hours of French by bofenghuang.
  # WER: 8.37% (much better than standard Whisper for French)
  # ─────────────────────────────────────────────────────────────────────────
  # model_size: "french-distil-dec2"  # Optimized FR model
  # model_size: "small"  # Multilingual model (good compromise speed/quality)
  model_size: "small" # Changed from large-v3-turbo (~10s latency) to small (~2.5s latency)

  # Language: "fr", "en", "auto" or empty "" for auto-detection
  language: "auto"
  
  # Prompt to guide transcription (helps even with forced language)
  # Provides context about the expected conversation style.
  # Note: Only used by Whisper, Canary doesn't use prompts.
  prompt: "Transcription of a conversation with an AI assistant."
  
  # Device: "cpu" or "cuda" (GPU)
  # ⚠️ With Qwen3-VL on GPU, use "cpu" for Whisper to avoid OOM
  # Whisper base on CPU is fast enough (~1-2s per phrase)
  device: "cpu"

# === Live2D Avatar ===
live2d:
  # Enable/disable Live2D overlay
  enabled: false
  
  # Model configuration
  model:
    # Path relative to project root
    path: "assets/models/march7th/"
    # Model settings file name
    settings_file: "march 7th.model3.json"
  
  # Window settings
  window:
    width: 400
    height: 600
    # Position: -1 = auto (bottom-right corner)
    x: -1
    y: -1
    opacity: 0.95
    frameless: true
    on_top: true
    transparent: true
  
  # Expression mapping from emotions to model expressions
  # Format: emotion_name: "expression_name"
  default_expression: "happy"
  expressions:
    happy: "星星"        # Stars
    excited: "比耶"      # Peace sign
    sad: "哭"           # Cry
    angry: "黑脸"        # Dark face
    shy: "脸红"          # Blush
    embarrassed: "流汗"   # Sweat
    surprised: "比耶"
    photo: "照相"        # Camera
    facepalm: "捂脸"     # Face cover
  
  # Lip-sync settings
  lip_sync:
    # Smoothing factor (0.0 = instant, 1.0 = very smooth)
    smoothing: 0.3
    # Sensitivity multiplier
    sensitivity: 1.0
  
  # Hotkeys
  hotkeys:
    toggle_visibility: "F12"
    toggle_frameless: "F11"
