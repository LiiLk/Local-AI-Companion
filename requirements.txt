# Local AI Companion - Runtime dependencies
#
# Keep security-sensitive transitive lower bounds in constraints-security.txt.
# pip applies the constraints automatically when installing this file.
-c constraints-security.txt

# Async HTTP client (for communicating with Ollama)
httpx>=0.25.0

# YAML configuration management
pyyaml>=6.0

# Text-to-Speech
edge-tts>=6.1.0      # Edge TTS (Microsoft cloud, fallback)
kokoro>=0.9.4        # Kokoro TTS (local, high quality)
soundfile>=0.12.0    # For saving WAV files

# Speech-to-Text (ASR)
faster-whisper>=1.0.0  # Optimized Whisper (4x faster)
sounddevice>=0.4.0     # Microphone audio capture
huggingface_hub>=0.20.0  # For downloading optimized French models

# Web Server
fastapi>=0.136.1       # Async web framework
uvicorn>=0.24.0        # ASGI server
websockets>=12.0       # WebSocket support
python-multipart>=0.0.27  # For file uploads
torchaudio>=2.1.0      # Audio processing for TTS

# Live2D Avatar Overlay
PyQt6>=6.8.0           # Native transparent desktop shell
PyQt6-WebEngine>=6.8.0 # Embedded Chromium view for Live2D shell
pywebview>=6.0.0       # Transparent window for Live2D avatar (6.0+ required for transparency)
pynput>=1.7.0          # Global hotkeys (F11, F12)
numpy>=1.26.4,<2.0     # Audio analysis for lip-sync; keep aligned with RVC worker

# Desktop Companion (optional)
pystray>=0.19.0        # System tray icon
pillow>=12.2.0         # Image handling for tray icon

# Language Detection (for auto voice switching)
langdetect>=1.0.9      # Language detection
emoji>=2.0.0           # Emoji removal for TTS
silero-vad
